import { BrowserRouter, Routes, Route } from "react-router";

export default function RoutesApp() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={""} />
      </Routes>
    </BrowserRouter>
  );
}
